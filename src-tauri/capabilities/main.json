{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Least-privilege capability for the bundled local launcher window.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:app:default"
  ]
}
