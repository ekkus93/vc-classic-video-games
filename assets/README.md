# Asset provenance

Every non-original asset added to the repository must be recorded in `ATTRIBUTION.json` before it is shipped.

See `docs/ASSET_POLICY.md` for the required provenance fields and clean-room rules.
